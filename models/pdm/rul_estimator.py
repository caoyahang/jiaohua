"""level3 RUL 剩余寿命估算 —— 本文件为占位，不实现。

V1.1 决议（方案 4.3.2，原文）：
  ⚠️ 焦化厂故障样本极少（单台设备年故障个位数），LSTM 退化模型/RUL
  在 2~3 年内没有足够样本训练，不做承诺式开发。

  后置项（积累 2~3 年故障样本后再立项）：
    level3 RUL 剩余寿命估算（LSTM+贝叶斯 / Wiener 退化过程）
    —— 届时需先盘点有效故障样本量，≥50 例再启动建模

启动检查清单（届时逐项确认后再建模块）：
  1. 全厂 P0/P1 设备有效故障样本 ≥ 50 例（含故障前完整退化段数据）
  2. 故障样本已打标：故障类型、故障时刻、退化起点
  3. 评估 LSTM+贝叶斯 与 Wiener 退化过程两条技术路线
  4. 先做单台 P0 设备（干熄焦循环风机优先）PoC，再推广

本文件存在的意义：占位 + 把决议写进代码库，防止有人提前"顺手实现"。
任何 PR 若在未满足上述条件时往本文件添加建模代码，应直接打回。
"""

MIN_FAILURE_SAMPLES_FOR_RUL = 50  # 立项门槛：有效故障样本数


def rul_project_ready(n_failure_samples: int) -> bool:
    """唯一的可执行函数：判断 RUL 是否达到立项条件。"""
    return n_failure_samples >= MIN_FAILURE_SAMPLES_FOR_RUL
